import 'package:flutter/material.dart';

class WaitConnectScreen extends StatelessWidget {
  static const String id = '/WaitConnect';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Wait for Connection')),
    );
  }
}
